通过System.out.println( "请输入工资：" );
        Scanner q = new Scanner(System.in);
        String e = q .nextLine();
        w = Integer.parseInt(e, 10);
方法取得用户输入的数据，并用后面的w取得数据的double型。

再通过一个if 
     then 
     else if
     end
来判断它的工资以及税收
因时间有限，方法比较暴力。


